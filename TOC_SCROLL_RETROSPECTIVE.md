# TOC 桌面端滚动修复复盘

## 产品摘要（非技术版）

本次问题的核心是：桌面端目录在长内容场景下，用户滚轮操作没有稳定落在目录内部，导致阅读时“目录不好用”。

本轮修复的结果是：

1. 桌面端目录在内容变长时可持续使用，不会因为条目增多而失去导航作用。
2. 目录与正文的阅读节奏更清晰，用户可以边看正文边快速定位下一篇。
3. 移动端体验保持原样，不引入新的学习成本。

业务层面价值：

1. 降低长日报场景下的阅读流失。
2. 提升目录作为“阅读导航器”的可用性和稳定性。
3. 减少同类反馈反复出现的概率，提升版本质量信心。

当前状态：问题已按目标修复并通过构建验证；后续会按回归清单持续监控长内容场景体验。

## 背景

问题现象：在桌面端，鼠标停留在目录卡片（TOC）区域滚轮滚动时，经常触发整页滚动，而不是目录内部滚动。随着文章数量和标题长度增加，目录项可见性与可点击性下降。

目标：

1. 保持现有单页结构与目录跳转功能不变。
2. 桌面端实现稳定的“侧栏 sticky + 内部滚动优先”。
3. 移动端保持原有自然页面滚动行为。

## 历史变更时间线（按提交）

1. `59548df`：移除 TOC 的 sticky 与 ScrollArea，回到普通流式布局。
2. `adb550e`：恢复 sticky 与 ScrollArea，尝试让 TOC 可滚动。
3. `ed918ba`：加入 `shrink-0` 与 `max-h` 相关约束，继续调 TOC 布局。
4. `6225c79`：把高度策略切到 `ScrollArea` 固定高度/最大高度方案。
5. `2ad23c4`：再切回“卡片限高 + ScrollArea flex 填充”方案。
6. `9f2ea43`：补上 `md:overflow-hidden`，缓解溢出视觉问题。
7. `011f7af`：最终将桌面端 TOC 卡片改为确定高度，并增加滚动链隔离，解决内部滚动不稳定问题。

## 为什么之前多次修复仍未稳定生效

1. 高度策略反复切换：在“卡片限高”和“滚动区限高”之间多次切换，未形成稳定闭环。
2. `max-height` 不是确定高度：仅有上限时，子元素 `flex-1` 在部分布局下不能稳定形成可滚动视口。
3. 滚动链未隔离：滚轮在边界时容易外溢到页面根滚动，体感为“右侧内容在滚”。
4. 移动端掩盖问题：移动端本来就是整页滚动，不暴露桌面端 sticky 场景的缺陷。

## 本次最终修复（实验线路）

### 实验假设

要让桌面端滚轮稳定落在目录内部，必须满足：

1. 父容器有确定高度。
2. 子滚动区接管剩余高度。
3. 边界滚动不向上级容器串联。

### 代码变更

文件：`src/pages/DailyDigest.tsx`

1. TOC 卡片从桌面端 `md:max-h-[calc(100vh-5rem)]` 调整为 `md:h-[calc(100vh-5rem)]`。
2. 保留 `md:overflow-hidden`，确保父容器裁剪有效。
3. `ScrollArea` 保持 `md:flex-1 md:min-h-0`，并新增 `md:overscroll-contain` 抑制滚动链外溢。

## 最终方案抽象（可复用模板）

桌面端可滚动 sticky 侧栏建议固定为三段式：

1. 外层容器：`sticky + definite height + overflow-hidden`。
2. 中间滚动层：`flex-1 + min-h-0 + overflow/scroll area`。
3. 滚动边界策略：`overscroll-behavior: contain`。

移动端继续用自然流式滚动，不强行复用桌面 sticky 逻辑。

## 验证结果

1. 构建验证通过：`pnpm build`（前后端均成功）。
2. 分支同步：`HEAD` 与 `origin/main` 同步在 `011f7af`。
3. 行为预期：桌面端在鼠标悬停 TOC 时，优先内部滚动；移动端保持原有页面滚动。

## 防回归清单

1. 不再将 TOC 高度策略在“卡片限高”和“滚动区限高”之间混用。
2. 保持 `min-h-0`，避免 flex 子项无法压缩导致滚动失效。
3. 保持桌面端 `overscroll-contain`，防止滚轮链外溢。
4. 每次调整 TOC 样式后，至少在“长标题 + 20+ 条目录 + 桌面常见分辨率”下回归。
